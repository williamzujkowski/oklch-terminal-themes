/** Type declaration for `@williamzujkowski/oklch-terminal-themes/themes-slim.json`. See `themes.json.d.ts` for why these exist (#185). */
import type { SlimTheme } from '../dist/index.js';

declare const themes: SlimTheme[];
export default themes;
